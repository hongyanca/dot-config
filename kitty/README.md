#### Clone the repository to configuration folder

```shell
git clone https://github.com/hongyanca/kitty ~/.config/kitty
```

or

```shell
git clone git@github.com:hongyanca/kitty.git ~/.config/kitty
```
